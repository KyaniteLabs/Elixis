# Elixis Environment Configuration
# Copy to .env and fill in your values

# LLM Provider Settings
# Primary: llama-local on VPS (docker bridge gateway)
# Fallback: LM Studio via Tailscale
LLM_PROVIDER=openai
LLM_BASE_URL=http://172.19.0.1:8085/v1
LLM_FALLBACK_URL=http://100.66.225.85:1234/v1
LLM_MODEL=Qwen3.5-0.8B-Q4_K_M.gguf
LLM_CLASSIFY_MODEL=
LLM_API_KEY=dummy

# CORS
CORS_ORIGIN=https://elixis.kyanitelabs.tech

# Deployment (CI/CD — secrets go in GitHub, not here)
VPS_HOST=187.124.238.235
VPS_USER=root
